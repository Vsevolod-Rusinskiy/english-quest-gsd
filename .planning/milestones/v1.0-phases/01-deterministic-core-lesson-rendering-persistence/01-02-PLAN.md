---
phase: 01-deterministic-core-lesson-rendering-persistence
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/core/answer-checking/checkSingleChoice.ts
  - src/core/answer-checking/checkMatching.ts
  - src/core/answer-checking/checkOrderBuilder.ts
  - tests/fixtures/single-choice.fixture.json
  - tests/fixtures/order-builder.fixture.json
  - tests/core/answer-checking/checkSingleChoice.test.ts
  - tests/core/answer-checking/checkMatching.test.ts
  - tests/core/answer-checking/checkOrderBuilder.test.ts
autonomous: true
requirements: [EXERCISE-02, EXERCISE-03, EXERCISE-04, CHECK-02]

must_haves:
  truths:
    - "single-choice answers are graded by deterministic option-id comparison, no agent call (EXERCISE-02, CHECK-02)"
    - "matching answers are graded by deterministic pair-id set comparison against Lesson-1A.json's real 8-pair exercise, no agent call (EXERCISE-03, CHECK-02)"
    - "order-builder answers are graded by deterministic ordered-token comparison, no agent call (EXERCISE-04, CHECK-02)"
    - "single-choice and order-builder — which have zero real data in Lesson-1A.json — are covered by hand-authored fixture JSON that validates against the Zod schema (Pitfall 1)"
  artifacts:
    - path: "src/core/answer-checking/checkSingleChoice.ts"
      provides: "deterministic single-choice checker"
      exports: ["checkSingleChoice"]
    - path: "src/core/answer-checking/checkMatching.ts"
      provides: "deterministic matching pair-id checker"
      exports: ["checkMatching"]
    - path: "src/core/answer-checking/checkOrderBuilder.ts"
      provides: "deterministic order-builder ordered-token checker"
      exports: ["checkOrderBuilder"]
    - path: "tests/fixtures/single-choice.fixture.json"
      provides: "hand-authored single-choice exercise (no real data exists — Pitfall 1)"
    - path: "tests/fixtures/order-builder.fixture.json"
      provides: "hand-authored order-builder exercise (no real data exists — Pitfall 1)"
  key_links:
    - from: "tests/fixtures/single-choice.fixture.json"
      to: "src/core/lesson/lessonSchema.ts"
      via: "fixture must pass SingleChoiceExerciseSchema (proves the [ASSUMED] shape is internally consistent)"
      pattern: "single-choice"
    - from: "src/core/answer-checking/checkMatching.ts"
      to: "public/Lesson-1A.json"
      via: "checkMatching graded against the real ex019 8-pair exercise"
      pattern: "pairIds"
---

<objective>
Complete the deterministic answer-checking core by adding the three remaining checkers (single-choice, matching, order-builder) and the hand-authored fixture JSON that single-choice and order-builder require because Lesson-1A.json contains zero examples of either type.

Purpose: CHECK-02 requires all non-text exercise types to be graded by pure deterministic comparison with no agent involvement. This plan makes the core able to grade every one of the 4 exercise types; Plan 03 then wires the renderers that let a child actually play single-choice, matching, and order-builder exercises.

Output: Three pure checker functions with full unit coverage, plus fixture JSON that proves the [ASSUMED] single-choice/order-builder schema shapes are internally consistent and gradeable.
</objective>

## Phase Goal

**As a** child learner (Intermediate, unit 1A), **I want to** complete the full 1A lesson (theory + all 4 exercise types) with instant deterministic answer checking, **so that** I can learn and keep my exact place even after a page reload, with zero dependency on any AI.

This plan delivers the deterministic grading for the three non-text exercise types.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-deterministic-core-lesson-rendering-persistence/01-CONTEXT.md
@.planning/phases/01-deterministic-core-lesson-rendering-persistence/01-RESEARCH.md
@.planning/phases/01-deterministic-core-lesson-rendering-persistence/01-01-SUMMARY.md
@Lesson-1A.json

<interfaces>
<!-- From Plan 01-01 src/core/lesson/lessonSchema.ts (created there; import the inferred types, do not redefine): -->
<!--   Exercise (discriminated union), TextInputExercise, MatchingExercise, SingleChoiceExercise, OrderBuilderExercise -->
<!--   MatchingExercise.answerCheck = { mode:"pairIds", pairs: [{leftId,rightId}] }; plus leftItems[], rightOptions[] -->
<!--   SingleChoiceExercise.answerCheck = { mode:"choiceId", correctOptionId } [ASSUMED]; plus options:[{id,labelEn}] -->
<!--   OrderBuilderExercise.answerCheck = { mode:"orderedTokens", correctOrder:[] } [ASSUMED]; plus wordBank:[] -->
<!-- From Plan 01-01 src/core/answer-checking/checkTextInput.ts — checker return shape convention: { isCorrect: boolean, source: "core" } -->
<!-- Real matching data (ex019): 8 pairs picture-1..8 → knife/fork/napkin/glass/prawns/fried-eggs/strawberries/salt-and-pepper -->
<!-- RESEARCH.md Pattern 3 gives the checkOrderBuilder reference (join(" ") equality). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hand-author fixtures + single-choice and order-builder checkers</name>
  <files>tests/fixtures/single-choice.fixture.json, tests/fixtures/order-builder.fixture.json, src/core/answer-checking/checkSingleChoice.ts, src/core/answer-checking/checkOrderBuilder.ts, tests/core/answer-checking/checkSingleChoice.test.ts, tests/core/answer-checking/checkOrderBuilder.test.ts</files>
  <read_first>
    - .planning/phases/01-deterministic-core-lesson-rendering-persistence/01-RESEARCH.md (Pattern 1 SingleChoice/OrderBuilder [ASSUMED] schemas; Pattern 3 checkOrderBuilder reference; Pitfall 1 — these types have no real data, fixtures are mandatory)
    - .planning/phases/01-deterministic-core-lesson-rendering-persistence/01-01-SUMMARY.md (exact exported schema/type names and checker return-shape convention from Plan 01)
  </read_first>
  <behavior>
    - single-choice.fixture.json is a single valid single-choice exercise carrying all BaseExercise fields plus options:[{id,labelEn}] and answerCheck:{mode:"choiceId", correctOptionId}. It must pass SingleChoiceExerciseSchema.safeParse (assert this in the test). Use lesson-appropriate 1A content (e.g. a food-vocabulary single-choice) so it is realistic.
    - order-builder.fixture.json is a single valid order-builder exercise with wordBank:[] plus answerCheck:{mode:"orderedTokens", correctOrder:[]}. It must pass OrderBuilderExerciseSchema.safeParse. Content e.g. reorder words into "What are you doing tonight".
    - checkSingleChoice(exercise, selectedOptionId) → { isCorrect: selectedOptionId === exercise.answerCheck.correctOptionId, source:"core" }. Correct id → true; any other id → false; no agent, no network (CHECK-02, EXERCISE-02).
    - checkOrderBuilder(exercise, sequence: string[]) → { isCorrect: sequence.join(" ") === exercise.answerCheck.correctOrder.join(" "), source:"core" }. Exact-order match → true; wrong order or missing/extra token → false (CHECK-02, EXERCISE-04, Pattern 3).
  </behavior>
  <action>
    Write tests/fixtures/single-choice.fixture.json and tests/fixtures/order-builder.fixture.json as described in <behavior>, importing/matching the exact field set of the Plan 01 schema (all BaseExercise fields: exerciseId, catalogRef, catalogItemRef, sourceRef, skill, prompt, targetWords, targetGrammar, hint, topicImpact, type, plus the type-specific fields). These are the documented answer to Pitfall 1 — the [ASSUMED] shape made concrete and testable.
    Implement src/core/answer-checking/checkSingleChoice.ts and src/core/answer-checking/checkOrderBuilder.ts as pure functions returning { isCorrect, source:"core" }, importing the exercise types from the Plan 01 lessonSchema. No LLM, no network, no fuzzy comparison. Cite CHECK-02, EXERCISE-02, EXERCISE-04. For checkOrderBuilder copy the RESEARCH.md Pattern 3 reference (join(" ") equality).
    Write the two unit test files: each first asserts its fixture passes the corresponding Zod schema (proving the assumed shape is internally consistent), then exercises the checker's correct/incorrect cases against the fixture.
  </action>
  <verify>
    <automated>npx vitest run tests/core/answer-checking/checkSingleChoice.test.ts tests/core/answer-checking/checkOrderBuilder.test.ts</automated>
  </verify>
  <done>Both fixtures validate against their Zod schemas; checkSingleChoice and checkOrderBuilder grade correct/incorrect cases with source:"core", no agent involvement. Pitfall 1 is closed — the two data-less exercise types now have hand-authored fixtures and passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: matching checker (pair-id set comparison against real ex019)</name>
  <files>src/core/answer-checking/checkMatching.ts, tests/core/answer-checking/checkMatching.test.ts</files>
  <read_first>
    - .planning/phases/01-deterministic-core-lesson-rendering-persistence/01-RESEARCH.md (Pattern 1 MatchingExerciseSchema / PairIdsCheckSchema; Architectural Responsibility Map — checkMatching pure, no I/O)
    - Lesson-1A.json (ex019 — the real 8-pair matching exercise: picture-1..8 → knife/fork/napkin/glass/prawns/fried-eggs/strawberries/salt-and-pepper)
  </read_first>
  <behavior>
    - checkMatching(exercise, userPairs: {leftId,rightId}[]) → { isCorrect, source:"core" }. isCorrect is true only when the user's set of (leftId→rightId) pairs equals the exercise's answerCheck.pairs set — order-independent, but every leftId must map to its correct rightId. One wrong pair → false; a missing pair → false; an extra pair → false.
    - Tested against the REAL ex019 from Lesson-1A.json: the complete correct mapping → true; swapping picture-1's rightId from "knife" to "fork" → false; omitting one pair → false.
    - Comparison is deterministic (build a leftId→rightId map on each side and compare), no agent, no network (CHECK-02, EXERCISE-03).
  </behavior>
  <action>
    Implement src/core/answer-checking/checkMatching.ts: pure function importing MatchingExercise from the Plan 01 lessonSchema. Normalize both the expected pairs (exercise.answerCheck.pairs) and the user pairs into a leftId→rightId lookup and compare for exact equivalence (same key set, same mapped value per key). Return { isCorrect, source:"core" }. No LLM/network. Cite CHECK-02, EXERCISE-03.
    Write tests/core/answer-checking/checkMatching.test.ts loading ex019 from the real Lesson-1A.json (or an inline copy of its answerCheck.pairs) and covering the correct-mapping, one-wrong-pair, and missing-pair cases from <behavior>.
  </action>
  <verify>
    <automated>npx vitest run tests/core/answer-checking/checkMatching.test.ts</automated>
  </verify>
  <done>checkMatching grades the real 8-pair ex019 correctly: full correct mapping passes, any wrong/missing/extra pair fails, all deterministic with source:"core" and no agent call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user selection/assembly → checker | The child's choice/pairs/sequence is input the checker compares; comparison must be exact and deterministic, never delegated to an agent (Phase 1 core-only boundary) |
| fixture/lesson JSON → checker | Checkers receive already-Zod-validated exercise objects (validated at load in Plan 01); no re-parse of untrusted input happens here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Tampering | checker inputs (exercise objects) | low | accept | Checkers operate only on exercise objects already validated by `LessonSchema` at load (Plan 01, T-01-01). No new external input boundary is introduced in this plan; re-validation here would be redundant. Rationale: the untrusted-input boundary is closed upstream at load time; these are pure functions over trusted, typed data. |
| T-01-06 | Spoofing (correctness bypass) | checkSingleChoice / checkMatching / checkOrderBuilder | medium | mitigate | Grading is strict deterministic comparison (id equality / pair-set equality / ordered-token equality) with NO fuzzy/edit-distance path and NO agent — a wrong answer cannot be scored correct. Verified by the incorrect-case assertions in each checker's unit test. |
</threat_model>

<verification>
- `npx vitest run tests/core/answer-checking` — all four checker test files green (checkTextInput from Plan 01 plus the three added here).
- Each checker returns `source: "core"` and never imports any agent/network module (grep confirms zero `fetch`/`@anthropic` references in `src/core/answer-checking/`).
- single-choice and order-builder fixtures validate against their Plan 01 Zod schemas.
</verification>

<success_criteria>
- All three non-text exercise types are graded by pure deterministic comparison with no agent call (CHECK-02, EXERCISE-02, EXERCISE-03, EXERCISE-04).
- Pitfall 1 is closed: single-choice and order-builder have hand-authored, schema-valid fixtures and passing checker tests.
- matching is graded against the real 8-pair ex019 from Lesson-1A.json.
</success_criteria>

<output>
Create `.planning/phases/01-deterministic-core-lesson-rendering-persistence/01-02-SUMMARY.md` when done.
</output>
