---
phase: 04-progress-advisor-reward-advisor-parent-report
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/core/agents/parentReportGeneratorSchema.ts
  - src/core/agents/parentReportGenerator.ts
  - src/core/state/store.ts
  - src/core/lessonEngine.ts
  - src/ui/screens/SessionEndScreen.ts
  - src/main.ts
  - tests/core/agents/parentReportGenerator.test.ts
  - tests/core/lessonEngine.test.ts
  - tests/e2e/fullLessonTraversal.test.ts
autonomous: true
requirements: [PERSONAL-01, PERSONAL-02, PERSONAL-03, REPORT-01, REPORT-02]

must_haves:
  truths:
    - "At lesson completion, the child taps an explicit 'Показать итоги' action that triggers exactly one handleSessionEnd() call, during which Progress Advisor resolves FIRST (agent success or fallback either way producing a final core-decided recommendedFocus/suggestedDifficulty) and ONLY THEN is Parent Report Generator called, receiving that final recommendation as part of its input"
    - "The bare 'Урок завершён!' message is replaced by one combined SessionEndScreen showing both the child-facing recommendation/motivational text and the parent report together, in a single render after both sequential calls resolve"
    - "If Progress Advisor is unavailable, the session still ends with a valid next-focus/difficulty decision driven purely by threshold rules (PERSONAL-03), and difficultyMode is never written except via applyDifficultyGuardrails()"
    - "If Parent Report Generator is unavailable, the same report fields (exercises completed, correct count, struggling topics, review topics, rubles earned, one recommendation) render via a deterministic template with no agent-authored prose"
  artifacts:
    - src/core/agents/parentReportGeneratorSchema.ts
    - src/core/agents/parentReportGenerator.ts
    - src/ui/screens/SessionEndScreen.ts
  key_links:
    - "LessonEngine.handleSessionEnd(): await callProgressAdvisor(...) -> applyDifficultyGuardrails(...) -> await callParentReportGenerator(snapshot + FINAL recommendation) -> ONE session_end dispatch writing studentProfile.{confidenceScore, difficultyMode, lastRecommendedFocus, motivationSignals}"
    - "main.ts's else-branch (lesson-complete) gains a 'Показать итоги' button whose click handler awaits engine.handleSessionEnd() behind the existing unsubscribe/thinking-cue/resubscribe pattern, then renders SessionEndScreen with the transient (non-persisted) report/recommendation text"
---

<objective>
Implement the Parent Report Generator agent (SPEC.md §8.4, REPORT-01/REPORT-02), and complete Phase 4 by wiring `LessonEngine.handleSessionEnd()` — the sequential Progress Advisor -> guardrails -> Parent Report Generator orchestration (D-06/D-07) — into a single combined end-of-session screen that replaces the bare "Урок завершён!" message (D-05/D-08).

Purpose: Deliver REPORT-01/REPORT-02 and complete PERSONAL-01/02/03's end-to-end wiring (Plan 02 built the pure functions and Progress Advisor agent in isolation; this plan is where they get exercised for real, sequenced, and rendered). This is the last vertical slice of Phase 4 and depends on Plan 02's `callProgressAdvisor()`/`applyDifficultyGuardrails()`/`computeConfidenceScore()`.

Output: `src/core/agents/parentReportGeneratorSchema.ts`, `src/core/agents/parentReportGenerator.ts`, `LessonEngine.handleSessionEnd()`, a new `session_end` store action, `src/ui/screens/SessionEndScreen.ts`, and `main.ts` wiring replacing the lesson-complete else-branch.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-progress-advisor-reward-advisor-parent-report/04-CONTEXT.md
@.planning/phases/04-progress-advisor-reward-advisor-parent-report/04-RESEARCH.md
@.planning/phases/04-progress-advisor-reward-advisor-parent-report/04-PATTERNS.md
@.planning/phases/04-progress-advisor-reward-advisor-parent-report/04-02-SUMMARY.md

@src/core/agents/theoryTutor.ts
@src/core/agents/theoryTutorSchema.ts
@src/core/agents/progressAdvisor.ts
@src/core/agents/progressAdvisorSchema.ts
@src/core/personalization/confidenceScore.ts
@src/core/personalization/difficultyGuardrails.ts
@src/core/lessonEngine.ts
@src/core/state/store.ts
@src/core/state/progressSchema.ts
@src/main.ts
@src/ui/screens/TheoryScreen.ts
@src/ui/components/FeedbackBanner.ts
@tests/core/lessonEngine.test.ts
@tests/e2e/fullLessonTraversal.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parent Report Generator schema + thin agent wrapper</name>
  <files>src/core/agents/parentReportGeneratorSchema.ts, src/core/agents/parentReportGenerator.ts, tests/core/agents/parentReportGenerator.test.ts</files>
  <read_first>src/core/agents/theoryTutor.ts, src/core/agents/theoryTutorSchema.ts, SPEC.md §8.4 and §13</read_first>
  <behavior>
    - Test 1 (agent success): a stubbed client resolving a tool_use response with `{parentReportRu, headlineRu}` (both strings) resolves `callParentReportGenerator(...)` to the same fields plus `source: "agent"`.
    - Test 2 (agent failure, both attempts reject, REPORT-02): resolves to a TEMPLATE-built `parentReportRu` deterministically interpolating the caller-supplied snapshot fields (exercisesCompleted, correctCount, strugglingTopics, reviewTopics, rublesEarned, recommendation) — assert the template string CONTAINS the numeric values passed in (e.g. `expect(result.parentReportRu).toContain(String(input.exercisesCompleted))`); `source: "core"`.
    - Test 3 (wrong-shape response, e.g. `headlineRu` missing): rejected by Zod into the same template fallback as Test 2; assert the stub client's `create` was called exactly twice.
  </behavior>
  <action>
    Create `src/core/agents/parentReportGeneratorSchema.ts` mirroring `theoryTutorSchema.ts`'s discipline: `ParentReportGeneratorResponseSchema = z.object({ parentReportRu: z.string(), headlineRu: z.string() })` per SPEC.md §8.4's exact contract. Export `type ParentReportGeneratorResponse = z.infer<...>`.

    Create `src/core/agents/parentReportGenerator.ts` mirroring `theoryTutor.ts`'s wrapper shape. Export `interface ParentReportInput { exercisesCompleted: number; correctCount: number; strugglingTopics: string[]; reviewTopics: string[]; rublesEarned: number; recommendation: string; client?: AgentClient }` — per SPEC.md §8.4's "снимок урока (пройдено, верно, ошибки, слабые темы, рубли) + рекомендация" input, where `recommendation` MUST be the caller's FINAL, core-decided value (documented in a comment: never the agent's raw Progress Advisor suggestion — the caller, `handleSessionEnd()` in Task 2, is responsible for passing the guardrailed value, not this wrapper). `userContent` is `JSON.stringify` of exactly these 6 data fields, never interpolated into the system prompt. System prompt: SPEC.md §13's tone requirement translated into English instructions — "просто, честно, с поддержкой, без терминов" (simple, honest, supportive, no jargon) — reporting via a single tool (e.g. `report_parent_summary`).

    Export `interface ParentReportResult { parentReportRu: string; headlineRu: string; source: "agent" | "core" }`. On agent success, map fields with `source: "agent"`. On fallback (REPORT-02), build a deterministic Russian template string interpolating ALL 6 input fields directly (e.g. "Ребёнок выполнил {exercisesCompleted} заданий, {correctCount} верно. Даётся сложнее: {strugglingTopics.join(', ') || 'нет'}. Повторить: {reviewTopics.join(', ') || 'нет'}. Заработано {rublesEarned} ₽. Рекомендация: {recommendation}.") and a fixed generic `headlineRu` (e.g. "Итоги урока") — this is REPORT-02's literal requirement: same fields, no agent text, template-only.
  </action>
  <verify>
    <automated>npm run test:core -- tests/core/agents/parentReportGenerator.test.ts</automated>
  </verify>
  <done>All 3 behavior cases pass; the fallback template string is fully deterministic (no randomness, no agent call artifacts) and contains every one of the 6 input snapshot fields somewhere in its output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LessonEngine.handleSessionEnd() — sequential Progress Advisor -> guardrails -> Parent Report orchestration, single session_end dispatch</name>
  <files>src/core/state/store.ts, src/core/lessonEngine.ts, tests/core/lessonEngine.test.ts</files>
  <read_first>src/core/lessonEngine.ts (handleTheoryStep/handleAnswer as the async-then-single-dispatch template), src/core/state/store.ts, src/core/agents/progressAdvisor.ts, src/core/personalization/difficultyGuardrails.ts, src/core/personalization/confidenceScore.ts, src/core/agents/parentReportGenerator.ts</read_first>
  <behavior>
    - Test 1 (sequential call order, D-07 — THE critical case): mock `callProgressAdvisor` to resolve after a delay (or use call-order assertion) and mock `callParentReportGenerator` to capture its received `recommendation` argument — assert `callParentReportGenerator` is invoked with a `recommendation` value that equals the ALREADY-RESOLVED Progress Advisor result's `recommendedFocus`, AND assert `callParentReportGenerator` was NOT called until after `callProgressAdvisor`'s promise resolved (e.g. via a shared ordering array both mocks push into, or `mockImplementationOnce` with explicit resolve ordering).
    - Test 2 (PERSONAL-02 guardrail applied, not bypassed): mock `callProgressAdvisor` to resolve with `suggestedDifficulty: "challenge"` while state's current `difficultyMode` is `"easy"` and no correct-streak signal present — assert the resulting `session_end` dispatch's `difficultyMode` is `"normal"` (one step, per Plan 02's guardrail), NEVER `"challenge"` directly.
    - Test 3 (PERSONAL-03, Progress Advisor unavailable): mock `callProgressAdvisor` to resolve fallback-shaped (`source: "core"`) — assert `handleSessionEnd()` still produces a valid `recommendedFocus`/`difficultyMode` decision (not undefined/crash), and `progressAdvisorFailed: true` is recorded on the dispatch.
    - Test 4 (REPORT-02, Parent Report Generator unavailable): mock `callParentReportGenerator` to resolve fallback-shaped (`source: "core"`) — assert the dispatch's `parentReportRu` is the deterministic template text (non-empty, containing snapshot data) and `parentReportFailed: true` is recorded.
    - Test 5 (single dispatch invariant): `setItemSpy`-style save-count assertion — `handleSessionEnd()` results in exactly ONE additional `localStorage.setItem` call (one dispatch = one save), never two.
    - Test 6 (confidenceScore computed and persisted): after `handleSessionEnd()`, `store.getState().studentProfile.confidenceScore` is a number in `[0, 1]` matching `computeConfidenceScore()`'s formula applied to the session's actual `exerciseStats`/`currentCorrectStreak`/`currentErrorStreak`.
  </behavior>
  <action>
    In `src/core/state/store.ts`, add a NEW `session_end` Action variant (this is the one legitimate new dispatch type in Phase 4 — session-end is a distinct user-visible event from any single exercise answer, so it does not fold into `exercise_attempt`): fields `confidenceScore: number`, `difficultyMode: DifficultyMode`, `recommendedFocus: string`, `motivationalMessageRu: string`, `parentReportRu: string`, `headlineRu: string`, `progressAdvisorSource: "core" | "agent"`, `progressAdvisorFailed: boolean`, `parentReportSource: "core" | "agent"`, `parentReportFailed: boolean`. Add the `reduce()` case: spread previous state, override `studentProfile` with `{...state.studentProfile, confidenceScore: action.confidenceScore, difficultyMode: action.difficultyMode, lastRecommendedFocus: action.recommendedFocus, motivationSignals: [...]}` (compute `motivationSignals` per D-CONTEXT's discretion — reuse Plan 02's chosen simple string-tag shape; derive at minimum a streak/error-based tag list from the session's final `currentCorrectStreak`/`currentErrorStreak`). Do NOT persist `motivationalMessageRu`/`parentReportRu`/`headlineRu` into `ProgressStateSchema` (Assumption A3: transient, in-memory only) — instead, have `handleSessionEnd()` RETURN these transient fields directly to its caller (mirrors `handleTheoryStep`'s `TheoryStepResult` return-value pattern for the same reason), while the `session_end` action itself still CARRIES them so the reducer has the data available if needed for the render callback, but the schema/persisted state only durably stores `confidenceScore`/`difficultyMode`/`lastRecommendedFocus`/`motivationSignals`.

    In `src/core/lessonEngine.ts`, add `async handleSessionEnd(): Promise<SessionEndResult>` (new exported interface `SessionEndResult { recommendedFocus: string; motivationalMessageRu: string; suggestedDifficulty: DifficultyMode; parentReportRu: string; headlineRu: string }` or similar — whatever main.ts needs to render). Implementation, following RESEARCH.md's Pattern 2 exactly:
    1. Read current `state = this.store.getState()`.
    2. Compute the caller-side threshold fallback inputs Progress Advisor's wrapper needs: `fallbackRecommendedFocus` = the `topicStats` entry with the worst correct/attempts ratio (or a fixed generic string if `topicStats` is empty) — this is core threshold logic, keep it inline or in a small local helper in this file, not a new module.
    3. `const advisorResult = await callProgressAdvisor({ topicStats: state.topicStats, wordStats: state.wordStats, exerciseTypeStats: state.exerciseTypeStats, currentDifficultyMode: state.studentProfile.difficultyMode, fallbackRecommendedFocus })`.
    4. `const finalDifficulty = applyDifficultyGuardrails(state.studentProfile.difficultyMode, advisorResult.suggestedDifficulty, { correctStreak: state.currentCorrectStreak, recentErrors: state.currentErrorStreak })` — THIS is the only place `difficultyMode`'s next value is decided; `advisorResult.suggestedDifficulty` is never assigned directly anywhere else.
    5. Compute `correctRatio` from `state.exerciseStats` (sum of `correct` / sum of `attempts` across all entries, per RESEARCH.md Assumption A2) and call `computeConfidenceScore({ correctRatio, streak: state.currentCorrectStreak, errorsInARow: state.currentErrorStreak })`.
    6. Compute the Parent Report snapshot: `exercisesCompleted = Object.keys(state.exerciseStats).length`, `correctCount = ` sum of `.correct` across `exerciseStats` (or count of exercises whose `lastAttemptCorrect` is true — pick one, document it), `strugglingTopics` = `topicStats` entries with `status: "needs_review"`, `reviewTopics` = `state.reviewQueue` (should be empty at true session end, but pass whatever remains defensively), `rublesEarned = state.currentRewards`.
    7. `const reportResult = await callParentReportGenerator({ exercisesCompleted, correctCount, strugglingTopics, reviewTopics, rublesEarned, recommendation: advisorResult.recommendedFocus })` — called ONLY after step 3-4 fully resolve (sequential `await`, never `Promise.all`, per D-07/anti-pattern).
    8. Dispatch ONE `session_end` action carrying all computed fields plus `progressAdvisorSource: advisorResult.source`, `progressAdvisorFailed: advisorResult.source === "core"`, `parentReportSource: reportResult.source`, `parentReportFailed: reportResult.source === "core"`.
    9. Return the transient render-facing result (recommendedFocus, motivationalMessageRu, suggestedDifficulty = finalDifficulty, parentReportRu, headlineRu) to the caller.
  </action>
  <verify>
    <automated>npm run test:core -- tests/core/lessonEngine.test.ts</automated>
  </verify>
  <done>All 6 behavior cases pass; `applyDifficultyGuardrails` is the only code path assigning `difficultyMode`; Parent Report Generator is never called before Progress Advisor's promise resolves; exactly one save() per `handleSessionEnd()` call.</done>
</task>

<task type="auto">
  <name>Task 3: SessionEndScreen + main.ts wiring (replace "Урок завершён!" with the combined screen, D-05/D-08)</name>
  <files>src/ui/screens/SessionEndScreen.ts, src/main.ts, tests/e2e/fullLessonTraversal.test.ts</files>
  <read_first>src/ui/screens/TheoryScreen.ts, src/ui/components/FeedbackBanner.ts, src/main.ts (the else-branch at the current "Урок завершён!" location, and the onSubmit/onUnderstoodChoice async-handler + unsubscribe/thinking-cue/resubscribe pattern), tests/e2e/fullLessonTraversal.test.ts</read_first>
  <action>
    Create `src/ui/screens/SessionEndScreen.ts` mirroring `TheoryScreen.ts`'s `createElement`/`textContent`-only discipline (never `innerHTML`, per every existing UI file's established convention). Export `renderSessionEndScreen(props: { recommendedFocus: string; motivationalMessageRu: string; suggestedDifficulty: DifficultyMode; parentReportRu: string; headlineRu: string; rublesEarned: number }): HTMLElement` building a single container with: a child-facing section (motivational message + recommended focus, e.g. as `<p>` elements with `textContent`), and a parent-facing section (headline + report text). No visual polish is required this phase (Phase 5 owns that per RESEARCH.md's Specific Ideas note) — plain semantic structure with distinguishing class names (`.session-end-screen`, `.child-section`, `.parent-section`) is sufficient.

    In `src/main.ts`: replace the existing lesson-complete `else` branch's bare `"Урок завершён!"` `<p>` with a "Показать итоги" button (per RESEARCH.md Pitfall 6's recommended option (a) — an explicit user tap is the trigger, avoiding any render-time auto-firing side effect) UNLESS `handleSessionEnd()`'s result is already available in memory (track this via a new in-memory variable, e.g. `let sessionEndResult: SessionEndResult | null = null`, mirroring `currentExplanation`'s existing transient-state pattern) — once available, render `SessionEndScreen` instead of the button. The button's click handler follows the EXACT unsubscribe/thinking-cue/resubscribe shape already used by `onSubmit`/`onUnderstoodChoice` (disable the button before the await, unsubscribe render, `await engine.handleSessionEnd()`, resubscribe in a `finally`, store the result in `sessionEndResult`, call `render(store.getState())` once). Keep the existing feedback-banner-before-done-message logic (the `feedbackAppliesHere` block) intact ABOVE the new button/screen — the banner for the final answered exercise must still show before the "Показать итоги" affordance appears.

    Extend `tests/e2e/fullLessonTraversal.test.ts` with one new test: complete a full session (reusing the existing traversal helper/pattern in that file) to the lesson-complete state, invoke the session-end trigger (calling `engine.handleSessionEnd()` directly if the e2e test drives the engine rather than simulated DOM clicks — check the existing file's style and match it), and assert the final store state has a valid `studentProfile.confidenceScore`/`difficultyMode` and that the returned transient result contains non-empty `parentReportRu`/`recommendedFocus`.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>The lesson-complete screen no longer shows the bare "Урок завершён!" text as its final state — it shows a "Показать итоги" affordance that, once tapped, renders the combined SessionEndScreen with both child recommendation and parent report data; full suite (including the new e2e case) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| LessonEngine <-> Parent Report Generator (via callAgent) | Same shared trust boundary as the other 4 agents — untrusted LLM router response, validated by `callAgent()`'s Zod safeParse |
| Progress Advisor's resolved output <-> Parent Report Generator's input | Parent Report Generator receives ONLY the FINAL, core/guardrail-decided `recommendedFocus` (post-`applyDifficultyGuardrails`), never the agent's raw, unvalidated suggestion — enforced structurally by `handleSessionEnd()`'s sequential await chain (D-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Tampering | `parentReportGenerator.ts` userContent construction | medium | mitigate | Lesson snapshot + recommendation passed as `JSON.stringify(...)` DATA in `userContent`, never interpolated into `systemPrompt` |
| T-04-08 | Denial of Service | Sequential Progress Advisor + Parent Report Generator calls (~32s worst case) blocking the session-end screen | low | accept | D-08's explicit, accepted tradeoff — session-end thinking-cue (button disabled + unsubscribe/resubscribe pattern, identical to Phase 3's precedent) covers the wait; not a mid-lesson blocking path, one-time at session end |
| T-04-09 | Tampering / Information Disclosure | Parent Report Generator's fallback template leaking raw internal field names or unescaped values into rendered HTML | low | mitigate | `SessionEndScreen.ts` uses `textContent` exclusively (never `innerHTML`), matching every existing UI file's established convention — the template string is plain text, never parsed as markup |
| T-04-10 | Elevation of Privilege | `session_end` dispatch writing `difficultyMode` from a value other than `applyDifficultyGuardrails()`'s return | high | mitigate | `handleSessionEnd()`'s only assignment path for `finalDifficulty` is the guardrail function's return value (verified by Plan 02/03's dedicated guardrail-bypass test case, Task 2 Test 2 above) |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages introduced this plan |

</threat_model>

<verification>
- `npm run test:core` — full core suite green, including new `parentReportGenerator.test.ts` and extended `lessonEngine.test.ts` session-end cases
- `npm test` — full suite (including the new e2e session-end case) green
</verification>

<success_criteria>
- Parent Report Generator is a thin `callAgent()` wrapper with a fully deterministic template fallback covering all 6 snapshot fields (REPORT-01/REPORT-02)
- `handleSessionEnd()` calls Progress Advisor, applies guardrails, and ONLY THEN calls Parent Report Generator with the FINAL recommendation — never in parallel, never with the raw agent suggestion (D-06/D-07)
- `difficultyMode` is written exclusively via `applyDifficultyGuardrails()`'s return value, with no bypass path (PERSONAL-02)
- The bare "Урок завершён!" message is replaced by an explicit "Показать итоги" affordance leading to the combined SessionEndScreen (D-05/D-08)
- Exactly one `session_end` dispatch per session-end trigger; agent/fallback availability for either agent never crashes or stalls the flow (PERSONAL-03/REPORT-02)
- Full test suite (including e2e) green
</success_criteria>

<output>
Create `.planning/phases/04-progress-advisor-reward-advisor-parent-report/04-03-SUMMARY.md` when done
</output>
