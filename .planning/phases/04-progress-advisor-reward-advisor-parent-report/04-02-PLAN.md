---
phase: 04-progress-advisor-reward-advisor-parent-report
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/core/state/progressSchema.ts
  - src/core/state/initialState.ts
  - src/core/progress/evaluateAttempt.ts
  - src/core/state/store.ts
  - src/core/lessonEngine.ts
  - src/core/personalization/confidenceScore.ts
  - src/core/personalization/difficultyGuardrails.ts
  - src/core/agents/progressAdvisorSchema.ts
  - src/core/agents/progressAdvisor.ts
  - tests/core/state/progressSchema.test.ts
  - tests/core/state/persistence.test.ts
  - tests/core/progress/evaluateAttempt.test.ts
  - tests/core/personalization/confidenceScore.test.ts
  - tests/core/personalization/difficultyGuardrails.test.ts
  - tests/core/agents/progressAdvisor.test.ts
autonomous: true
requirements: [PERSONAL-01, PERSONAL-02, PERSONAL-03]

must_haves:
  truths:
    - "The schema tracks wordStats (per-word attempts/correct/errors, looping ALL targetWords entries per exercise, never just index 0) and exerciseTypeStats (per exercise-type attempts/correct), plus studentProfile.confidenceScore/difficultyMode/lastRecommendedFocus/motivationSignals, and a new session-global currentErrorStreak counter"
    - "confidenceScore is computed by one pure function using SPEC.md §12's exact formula, independently unit-tested against known input/output pairs"
    - "difficultyMode is written ONLY by applyDifficultyGuardrails() — the Progress Advisor's suggestedDifficulty is one input to that function, never a direct assignment to studentProfile.difficultyMode; the guardrail blocks any easy<->challenge jump and only allows one-step transitions gated on 3-correct-streak (up) / 2-errors (down)"
    - "A pre-Phase-4-shaped localStorage blob (missing wordStats/exerciseTypeStats/the new studentProfile fields/currentErrorStreak) resets to fresh initialState() on load, per the established required-fields discipline — it never partially validates"
  artifacts:
    - src/core/personalization/confidenceScore.ts
    - src/core/personalization/difficultyGuardrails.ts
    - src/core/agents/progressAdvisorSchema.ts
    - src/core/agents/progressAdvisor.ts
  key_links:
    - "evaluateAttempt() extended to also loop exercise.targetWords into a wordUpdates accumulator (mirrors the existing topicImpact loop exactly) and to update exerciseTypeStats[exercise.type], returned alongside the existing topicUpdates/reviewQueueAdditions/rewardEvents/nextCorrectStreak fields"
    - "difficultyGuardrails.applyDifficultyGuardrails(current, suggested, signals) is the ONLY function callers may use to compute the next difficultyMode; progressAdvisor.ts itself never writes difficultyMode, only proposes suggestedDifficulty"
    - "progressAdvisorSchema.ts's ProgressAdvisorResponseSchema matches SPEC.md §8.2's contract (recommendedFocus, suggestedDifficulty, reviewSuggestions, motivationalMessageRu, sessionAdvice)"
---

<objective>
Build the state-schema foundation Progress Advisor needs (`wordStats` — D-11: a dedicated wordStats record IS built for Progress Advisor's input, separate from topicStats/exerciseTypeStats — `exerciseTypeStats`, `currentErrorStreak`, and `studentProfile` extensions), the two pure guardrail/formula functions SPEC.md §12 requires (`confidenceScore`, `difficultyMode` transition rules — D-09: difficultyMode is computed and stored only this phase, an informational value with no in-session observable effect given the MVP's single lesson, but the guardrail computation itself must still be correct), and the Progress Advisor agent itself as a thin `callAgent()` wrapper — all without wiring session-end orchestration or UI yet (that is Plan 03).

Purpose: Deliver PERSONAL-01 (agent recommendation from topicStats/wordStats/exerciseTypeStats), PERSONAL-02 (core-enforced difficulty guardrails independent of the agent's suggestion), and PERSONAL-03 (threshold-only fallback when the agent is unavailable) as an isolated, fully-testable core+agent slice. Plan 03 (Wave 3) consumes this plan's `callProgressAdvisor()` and `applyDifficultyGuardrails()` when it builds the session-end orchestration.

Output: Extended `ProgressStateSchema`/`initialState()`, extended `evaluateAttempt()` producing `wordUpdates`/`exerciseTypeUpdates`, `src/core/personalization/confidenceScore.ts`, `src/core/personalization/difficultyGuardrails.ts`, `src/core/agents/progressAdvisorSchema.ts`, `src/core/agents/progressAdvisor.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-progress-advisor-reward-advisor-parent-report/04-CONTEXT.md
@.planning/phases/04-progress-advisor-reward-advisor-parent-report/04-RESEARCH.md
@.planning/phases/04-progress-advisor-reward-advisor-parent-report/04-PATTERNS.md

@src/core/state/progressSchema.ts
@src/core/state/initialState.ts
@src/core/state/persistence.ts
@src/core/progress/evaluateAttempt.ts
@src/core/progress/topicStatusMachine.ts
@src/core/rewards/rewardEngine.ts
@src/core/agents/theoryTutor.ts
@src/core/agents/theoryTutorSchema.ts
@src/core/agents/callAgent.ts
@src/core/lesson/lessonSchema.ts
@tests/core/progress/topicStatusMachine.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ProgressStateSchema/initialState with wordStats, exerciseTypeStats, currentErrorStreak, studentProfile fields; extend evaluateAttempt's per-word/per-type loops</name>
  <files>src/core/state/progressSchema.ts, src/core/state/initialState.ts, src/core/progress/evaluateAttempt.ts, tests/core/state/progressSchema.test.ts, tests/core/state/persistence.test.ts, tests/core/progress/evaluateAttempt.test.ts</files>
  <read_first>src/core/state/progressSchema.ts (TopicStatSchema/StudentProfileSchema/ProgressStateSchema), src/core/state/initialState.ts, src/core/progress/evaluateAttempt.ts (the topicImpact loop, lines 46-75), tests/core/progress/evaluateAttempt.test.ts, public/Lesson-1A.json (eq-1a-ex019's 8-word targetWords)</read_first>
  <behavior>
    - Test 1 (schema shape): `WordStatSchema` accepts `{attempts, correct, errors}`; `ExerciseTypeStatSchema` accepts `{attempts, correct}`; `DifficultyModeSchema` accepts only `"easy" | "normal" | "challenge"`; extended `StudentProfileSchema` requires `confidenceScore` (number), `difficultyMode` (DifficultyModeSchema), `lastRecommendedFocus` (string or null — planner's discretion on nullability, document choice), `motivationSignals` (array, per CONTEXT.md A4 discretion).
    - Test 2 (legacy blob reset, Pitfall 1): a pre-Phase-4-shaped stored blob (valid Phase 3 shape, missing `wordStats`/`exerciseTypeStats`/`currentErrorStreak`/new `studentProfile` fields) fails `StoredBlobSchema.safeParse` and `load()` resets to `initialState()` — extend `tests/core/state/persistence.test.ts` with this exact case.
    - Test 3 (evaluateAttempt wordStats loop, Pitfall 4 — THE critical case): using the REAL `eq-1a-ex019` matching exercise fixture (8 `targetWords` entries, loaded from `public/Lesson-1A.json` exactly as `tests/core/lessonEngine.test.ts` already does), assert `evaluateAttempt(...)`'s returned `wordUpdates` contains an entry for ALL 8 words, each incremented correctly — NOT just `targetWords[0]`.
    - Test 4 (evaluateAttempt wordStats, single-word case): a 1-word exercise (e.g. `eq-1a-ex010`, `targetWords: ["meat"]`) produces exactly one `wordUpdates` entry.
    - Test 5 (evaluateAttempt exerciseTypeStats): a `text-input` exercise attempt increments `exerciseTypeUpdates["text-input"].attempts`/`.correct` correctly relative to any pre-existing `state.exerciseTypeStats["text-input"]`.
    - Test 6 (accumulator-first-fallback-to-state discipline): mirrors the existing `topicUpdates[topic] ?? state.topicStats[topic] ?? DEFAULT` pattern — verify a duplicate word within one exercise's (hypothetical/schema-legal) `targetWords` array is handled correctly (reads from the in-progress accumulator first), matching D-12's explicit requirement.
  </behavior>
  <action>
    In `src/core/state/progressSchema.ts`: add `WordStatSchema = z.object({ attempts: z.number(), correct: z.number(), errors: z.number() })` mirroring `TopicStatSchema`'s field style minus `status`/`correctStreak`. Add `ExerciseTypeStatSchema = z.object({ attempts: z.number(), correct: z.number() })`. Add `DifficultyModeSchema = z.enum(["easy", "normal", "challenge"])` mirroring `TopicStatusSchema`'s enum style. Extend `StudentProfileSchema` with `confidenceScore: z.number()`, `difficultyMode: DifficultyModeSchema`, `lastRecommendedFocus: z.string().nullable()`, `motivationSignals: z.array(z.string())` (a simple string-tag array is the lowest-risk shape per CONTEXT.md's explicit discretion grant — document this choice in a code comment citing A4; do not over-engineer a richer object shape). Extend `ProgressStateSchema` with `wordStats: z.record(z.string(), WordStatSchema)`, `exerciseTypeStats: z.record(z.string(), ExerciseTypeStatSchema)` (sparse records, matching `topicStats`'s established convention per RESEARCH.md Pitfall 5 — NOT a fully-keyed `Record<Exercise["type"], ...>`), and `currentErrorStreak: z.number()` (a new session-global counter mirroring `currentCorrectStreak`'s exact shape/reset semantics but tracking consecutive INCORRECT answers, per RESEARCH.md's Open Question 1 recommendation — resolved: add this new field). ALL new fields required, no `.optional()`, matching this file's established header-comment discipline (extend that comment to note Phase 4's additions).

    In `src/core/state/initialState.ts`: seed `wordStats: {}`, `exerciseTypeStats: {}`, `currentErrorStreak: 0`, and extend the `studentProfile` literal with `confidenceScore: 0, difficultyMode: "normal", lastRecommendedFocus: null, motivationSignals: []`.

    In `src/core/progress/evaluateAttempt.ts`: extend `EvaluateAttemptResult` with `wordUpdates: Record<string, WordStat>` and `exerciseTypeUpdates: Record<string, ExerciseTypeStat>`. Add a `wordUpdates` accumulator loop copying the EXACT accumulator-first-fallback-to-state pattern already used for `topicUpdates` (lines 53-75) but iterating `exercise.targetWords` — `for (const word of exercise.targetWords) { const prev = wordUpdates[word] ?? state.wordStats[word] ?? DEFAULT_WORD_STAT; wordUpdates[word] = { attempts: prev.attempts + 1, correct: prev.correct + (isCorrect ? 1 : 0), errors: prev.errors + (isCorrect ? 0 : 1) }; }` — never read `targetWords[0]` only; must loop the full array (Pitfall 4). Add a simple (non-looped) `exerciseTypeUpdates` computation: `const prevTypeStat = state.exerciseTypeStats[exercise.type] ?? DEFAULT_EXERCISE_TYPE_STAT; exerciseTypeUpdates[exercise.type] = { attempts: prevTypeStat.attempts + 1, correct: prevTypeStat.correct + (isCorrect ? 1 : 0) };`. Also compute the new session-global `nextErrorStreak` locally (mirrors `computeRewardEvents`'s existing `nextCorrectStreak` computation: increments on incorrect, resets to 0 on correct) and add it as a new returned field `nextErrorStreak: number` on `EvaluateAttemptResult` — this is core arithmetic belonging in this pure aggregator, not a new module, since it is trivial and directly parallels the existing `nextCorrectStreak` the reward engine already computes.

    Do NOT wire `wordUpdates`/`exerciseTypeUpdates`/`nextErrorStreak` into `store.ts`'s `exercise_attempt` action or `LessonEngine.handleAnswer`'s dispatch in this task — that wiring is Task 2 of THIS plan (evaluateAttempt is extended here; the dispatch/reduce wiring happens together with the confidenceScore/guardrail work below, since both belong to the same "personalization state" concern). Actually: wire the dispatch NOW as part of this task, since `evaluateAttempt`'s new return fields are meaningless until threaded through — extend `store.ts`'s `exercise_attempt` Action variant with `wordUpdates: Record<string, WordStat>`, `exerciseTypeUpdates: Record<string, ExerciseTypeStat>`, `nextErrorStreak: number`, extend the `reduce()` branch to spread them into `state.wordStats`/`state.exerciseTypeStats`/`state.currentErrorStreak` (same immutable-spread discipline as `topicStats`), and update `LessonEngine.handleAnswer`'s existing dispatch call to pass `delta.wordUpdates`, `delta.exerciseTypeUpdates`, `delta.nextErrorStreak` alongside the existing fields. This keeps the single-dispatch invariant intact — no new dispatch, only new fields on the existing one.
  </action>
  <verify>
    <automated>npm run test:core -- tests/core/state/progressSchema.test.ts tests/core/state/persistence.test.ts tests/core/progress/evaluateAttempt.test.ts</automated>
  </verify>
  <done>All 6 behavior cases pass; a legacy Phase-3-shaped blob resets on load; the 8-word `eq-1a-ex019` fixture updates all 8 `wordStats` entries, not just one.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: confidenceScore + difficultyGuardrails pure functions</name>
  <files>src/core/personalization/confidenceScore.ts, src/core/personalization/difficultyGuardrails.ts, tests/core/personalization/confidenceScore.test.ts, tests/core/personalization/difficultyGuardrails.test.ts</files>
  <read_first>src/core/progress/topicStatusMachine.ts (function signature + doc-comment style to mirror), src/core/rewards/rewardEngine.ts (file-header pure-function comment style), SPEC.md §12</read_first>
  <behavior>
    - Test 1 (confidenceScore, table-driven): `computeConfidenceScore({correctRatio, streak, errorsInARow})` returns `clamp(correctRatio + 0.05*streak - 0.1*errorsInARow, 0, 1)` for at least 5 representative input rows including a clamp-to-0 case (very low ratio, high errorsInARow) and a clamp-to-1 case (ratio 1, high streak).
    - Test 2 (difficultyGuardrails, same-mode no-op): `applyDifficultyGuardrails("normal", "normal", {correctStreak: 0, recentErrors: 0})` returns `"normal"`.
    - Test 3 (one-step-up allowed): current `"normal"`, suggested `"challenge"`, `correctStreak: 3` (meets the "up only after 3 correct in a row" gate) returns `"challenge"`.
    - Test 4 (one-step-up blocked, insufficient streak): current `"normal"`, suggested `"challenge"`, `correctStreak: 1` returns `"normal"` (unchanged — the up-gate wasn't met).
    - Test 5 (one-step-down allowed): current `"normal"`, suggested `"easy"`, `recentErrors: 2` (meets the "down only after 2 errors" gate) returns `"easy"`.
    - Test 6 (one-step-down blocked, insufficient errors): current `"normal"`, suggested `"easy"`, `recentErrors: 0` returns `"normal"`.
    - Test 7 (two-step jump blocked, both directions): current `"easy"`, suggested `"challenge"` (even with `correctStreak: 3`) returns `"normal"` (one step only, never a direct easy->challenge jump) OR returns `"easy"` (no change) — pick and assert ONE deterministic interpretation and document it in the function's doc comment (SPEC.md says "не прыгать easy→challenge напрямую" — jumping is forbidden, but whether a blocked jump advances one step or makes no change is an implementation choice; recommend one-step-at-a-time advancement toward the suggestion when the gate is met, capped to a single step per call, since PERSONAL-02's guardrail is evaluated once per session-end call per D-10). Same test mirrored for `"challenge"` -> suggested `"easy"` with `recentErrors: 2`.
    - Test 8 (insufficient signal, no other change): current `"easy"`, suggested `"normal"`, `correctStreak: 0` returns `"easy"` (no upward change without the streak gate).
  </behavior>
  <action>
    Create `src/core/personalization/confidenceScore.ts` copying RESEARCH.md's Code Examples section verbatim (the `clamp` helper + `computeConfidenceScore` function operating on `{correctRatio, streak, errorsInARow}`), with a file-header comment mirroring `rewardEngine.ts`'s "Pure function: NO network, NO agent call" discipline, citing SPEC.md §12's exact formula and this module as its sole implementation site.

    Create `src/core/personalization/difficultyGuardrails.ts` mirroring `topicStatusMachine.ts`'s function-signature and doc-comment style exactly: `export function applyDifficultyGuardrails(current: DifficultyMode, suggested: DifficultyMode, signals: { correctStreak: number; recentErrors: number }): DifficultyMode`. Implementation must enforce ALL of SPEC.md §12's rules as one small pure function: (a) no direct easy<->challenge jump — moving from `easy` toward `challenge` or vice versa is capped to exactly one step (`easy`->`normal` or `challenge`->`normal`) per call, never landing directly on the opposite extreme in one call; (b) upward movement (`normal`->`challenge` or `easy`->`normal`) requires `correctStreak >= 3`; (c) downward movement (`normal`->`easy` or `challenge`->`normal`) requires `recentErrors >= 2`; (d) if `suggested === current`, or if the relevant gate isn't met, return `current` unchanged — the agent's suggestion is NEVER returned verbatim without passing through this gate logic. Write an exhaustive doc-comment above the function (mirroring `topicStatusMachine.ts`'s WR-03-style rationale block) explicitly stating the interpretation chosen for the two-step-jump case (test 7) so the "why" is auditable, not just the "what". This function is the ONLY place `difficultyMode`'s next value is decided — `progressAdvisor.ts` (Task 3) never assigns `suggestedDifficulty` directly to any state field.
  </action>
  <verify>
    <automated>npm run test:core -- tests/core/personalization/confidenceScore.test.ts tests/core/personalization/difficultyGuardrails.test.ts</automated>
  </verify>
  <done>All 8 behavior cases pass; neither function performs any I/O, agent call, or state read/write beyond its explicit parameters.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Progress Advisor schema + thin agent wrapper</name>
  <files>src/core/agents/progressAdvisorSchema.ts, src/core/agents/progressAdvisor.ts, tests/core/agents/progressAdvisor.test.ts</files>
  <read_first>src/core/agents/theoryTutor.ts, src/core/agents/theoryTutorSchema.ts, tests/core/agents/theoryTutor.test.ts, SPEC.md §8.2, src/core/state/progressSchema.ts (TopicStat/WordStat/ExerciseTypeStat/DifficultyMode types from Task 1)</read_first>
  <behavior>
    - Test 1 (agent success): a stubbed client resolving a tool_use response with `{recommendedFocus, suggestedDifficulty, reviewSuggestions, motivationalMessageRu, sessionAdvice}` (all fields matching SPEC.md §8.2's contract) resolves `callProgressAdvisor(...)` to the same fields plus `source: "agent"`.
    - Test 2 (agent failure, both attempts reject, PERSONAL-03): resolves to a fallback shape where `recommendedFocus`/`suggestedDifficulty` are derived PURELY from the caller-supplied threshold inputs (e.g. the weakest topic by error count, and `currentDifficultyMode` unchanged as the suggestion) — never fabricated agent-style prose; `source: "core"`.
    - Test 3 (wrong-shape response, e.g. `suggestedDifficulty` outside the 3-value enum): rejected by Zod into the same fallback as Test 2; assert the stub client's `create` was called exactly twice (one retry).
  </behavior>
  <action>
    Create `src/core/agents/progressAdvisorSchema.ts` mirroring `theoryTutorSchema.ts`'s file-header discipline. Define `ProgressAdvisorResponseSchema = z.object({ recommendedFocus: z.string(), suggestedDifficulty: DifficultyModeSchema, reviewSuggestions: z.array(z.string()), motivationalMessageRu: z.string(), sessionAdvice: z.enum(["continue", "soft_finish"]) })` per SPEC.md §8.2's exact contract (`sessionAdvice` is explicitly `continue/soft_finish` per SPEC's literal wording — a 2-value enum, not a free string). Import `DifficultyModeSchema` from `progressSchema.ts` (Task 1), do not redefine it. Export `type ProgressAdvisorResponse = z.infer<typeof ProgressAdvisorResponseSchema>`.

    Create `src/core/agents/progressAdvisor.ts` mirroring `theoryTutor.ts`'s wrapper shape. Export `interface ProgressAdvisorInput { topicStats: Record<string, TopicStat>; wordStats: Record<string, WordStat>; exerciseTypeStats: Record<string, ExerciseTypeStat>; currentDifficultyMode: DifficultyMode; fallbackRecommendedFocus: string; client?: AgentClient }` — `fallbackRecommendedFocus` is caller-computed (PERSONAL-03: the core's OWN threshold-rule-derived weakest-topic value, e.g. the `topicStats` entry with the highest `errors`/lowest `correct` ratio, computed by the CALLER since that's session-aggregation logic, not this thin wrapper's concern) and used verbatim in the fallback branch, never fabricated. `userContent` is `JSON.stringify` of ONLY `topicStats`/`wordStats`/`exerciseTypeStats`/`currentDifficultyMode` (data fields per SPEC.md §8.2, never interpolated into the system prompt). System prompt: untrusted-data framing identical in spirit to `theoryTutor.ts`'s, reporting via a single tool (e.g. `report_progress_recommendation`). The `fallback` param passed to `callAgent()` is a `ProgressAdvisorResponse`-shaped value using `input.fallbackRecommendedFocus` for `recommendedFocus`, `input.currentDifficultyMode` for `suggestedDifficulty` (PERSONAL-03: no upward/downward suggestion without the agent — the guardrail function in Task 2 will see `suggested === current` and correctly no-op), an empty `reviewSuggestions` array, a fixed generic Russian `motivationalMessageRu`, and `sessionAdvice: "continue"`.

    Export `interface ProgressAdvisorResult` mirroring the response shape plus `source: "agent" | "core"`. On `result.source === "agent"`, return the mapped agent fields with `source: "agent"`. On fallback, return the exact fallback values constructed above with `source: "core"`. Critically: this wrapper NEVER calls `applyDifficultyGuardrails()` itself — it only proposes `suggestedDifficulty`; the guardrail application happens in the caller (Plan 03's `handleSessionEnd()`), per PERSONAL-02's "core enforces, independent of agent" requirement and the Architectural Responsibility Map's explicit separation of these two concerns into different modules.
  </action>
  <verify>
    <automated>npm run test:core -- tests/core/agents/progressAdvisor.test.ts</automated>
  </verify>
  <done>All 3 behavior cases pass; `progressAdvisor.ts` never imports or calls `applyDifficultyGuardrails`; fallback `suggestedDifficulty` always equals the caller-supplied `currentDifficultyMode` unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| LessonEngine (future caller) <-> Progress Advisor (via callAgent) | Same shared trust boundary as Phase 3 — untrusted LLM router response, validated by `callAgent()`'s Zod safeParse before any field is read |
| Progress Advisor's suggestedDifficulty <-> studentProfile.difficultyMode | The agent's suggestion is NEVER a direct write — `applyDifficultyGuardrails()` (built this plan, wired in Plan 03) is the sole writer, taking the suggestion as one input among several core-computed signals |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Tampering | `progressAdvisor.ts` userContent construction | medium | mitigate | `topicStats`/`wordStats`/`exerciseTypeStats`/`currentDifficultyMode` passed as `JSON.stringify(...)` DATA in `userContent`, never interpolated into `systemPrompt`; system prompt explicitly frames them as untrusted data |
| T-04-05 | Elevation of Privilege | Progress Advisor's `suggestedDifficulty` bypassing guardrails | high | mitigate | `applyDifficultyGuardrails()` is a standalone pure function that is the ONLY legitimate writer of `difficultyMode`'s next value; `progressAdvisor.ts` has no code path that assigns `suggestedDifficulty` to any persisted field directly (verified by this plan's Task 3 `<done>` criterion) |
| T-04-06 | Tampering | Schema growth (wordStats/exerciseTypeStats/studentProfile fields) breaking legacy saved progress | medium | mitigate | All new fields added as REQUIRED (no `.optional()`), matching `progressSchema.ts`'s established discipline; a legacy Phase-3-shaped blob fails `safeParse` and resets via `load()`'s existing fallback to `initialState()` — verified by an explicit persistence test case this plan adds |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages introduced this plan |

</threat_model>

<verification>
- `npm run test:core` — full core suite green, including new schema/persistence/evaluateAttempt/personalization/agent tests
- `npm test` — full suite (including e2e) green, confirming `wordUpdates`/`exerciseTypeUpdates`/`nextErrorStreak` wiring into the existing `exercise_attempt` dispatch does not regress Phase 1-3 traversal behavior
</verification>

<success_criteria>
- `wordStats`/`exerciseTypeStats`/`currentErrorStreak`/extended `studentProfile` fields exist in `ProgressStateSchema`, seeded in `initialState()`, and correctly update via `evaluateAttempt()` + the existing single dispatch
- The real 8-word `eq-1a-ex019` fixture updates all 8 `wordStats` entries per attempt, never just the first
- `computeConfidenceScore()` matches SPEC.md §12's formula exactly, unit-tested with a clamp-boundary table
- `applyDifficultyGuardrails()` blocks every prohibited transition (two-step jumps, insufficient-signal one-step moves) and allows every permitted one, fully unit-tested
- `callProgressAdvisor()` never calls or references `applyDifficultyGuardrails` — the two concerns stay in separate modules
- A legacy Phase-3-shaped blob resets cleanly on load rather than partially validating
- Full test suite green
</success_criteria>

<output>
Create `.planning/phases/04-progress-advisor-reward-advisor-parent-report/04-02-SUMMARY.md` when done
</output>
