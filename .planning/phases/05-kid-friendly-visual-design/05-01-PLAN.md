---
phase: 05-kid-friendly-visual-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/style.css
  - src/main.ts
  - src/ui/components/ThinkingIndicator.ts
  - src/ui/components/RewardToast.ts
  - src/ui/components/ProgressIndicator.ts
  - tests/main.test.ts
  - tests/ui/components/ThinkingIndicator.test.ts
  - tests/ui/components/ProgressIndicator.test.ts
autonomous: true
requirements: [UI-01, UI-02]

must_haves:
  truths:
    - "Top bar shows the ruble balance (state.currentRewards) as a visible chip, live-updating on every render"
    - "All 3 agent-call sites (theory buttons, exercise submit, 'Показать итоги') show a shared thinking-indicator element while awaiting, using the same component/class"
    - "A '+N ₽' reward toast appears when state.currentRewards increases across a handleAnswer() call, and auto-dismisses"
    - "Feedback banner is never shown for a render not caused by its own answer (D-12 Gap 1 fixed)"
    - "Progress indicator never shows 'N+1 из N' at lesson-complete (D-12 Gap 2 fixed)"
    - "All new/changed color, spacing, and typography token VALUES in style.css match 05-UI-SPEC.md exactly"
  artifacts:
    - src/ui/components/ThinkingIndicator.ts (renderThinkingIndicator())
    - src/ui/components/RewardToast.ts (renderRewardToast(amount))
    - src/ui/components/ProgressIndicator.ts (renderProgressIndicatorComplete(total), added alongside existing exports)
    - tests/main.test.ts (new)
    - tests/ui/components/ThinkingIndicator.test.ts (new)
  key_links:
    - "main.ts render() top-bar block reads state.currentRewards directly, same field SessionEndScreen already reads"
    - "main.ts onSubmit handler captures state.currentRewards before the handleAnswer() await and diffs after, to trigger the reward toast (HandleAnswerResult has NO rewardAmount field — see 05-PATTERNS.md correction)"
    - "main.ts top-bar completion check reuses engine.getCurrentExercise() as the single completion signal, not a second ad-hoc check"
</must_haves>
---

<objective>
Establish the Phase 5 visual foundation: update every CSS design-token VALUE in `src/style.css` to the bright/blocky/kid-friendly values specified in `05-UI-SPEC.md` (colors, typography, spacing, chunky-button shape/shadow, motion keyframes), add the two new shared components (`ThinkingIndicator`, `RewardToast`), wire them plus the ruble-balance top-bar chip into `main.ts`, and fix the two Phase 1 UAT bugs (D-12: feedback banner not clearing on advance; progress indicator overshoot at lesson-complete).

Purpose: every later per-screen re-skin task (Wave 2) depends on these tokens/components already existing and being correct — this plan is the single point where `style.css` and `main.ts` are touched, avoiding file-conflict across parallel Wave 2 plans.
Output: Updated `src/style.css` (new token values + new component classes + keyframes), new `src/ui/components/ThinkingIndicator.ts`, new `src/ui/components/RewardToast.ts`, extended `src/ui/components/ProgressIndicator.ts`, updated `src/main.ts` (ruble chip, 3 thinking-indicator call sites, reward-toast trigger, both D-12 bug fixes), new `tests/main.test.ts`, new `tests/ui/components/ThinkingIndicator.test.ts`, extended `tests/ui/components/ProgressIndicator.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-kid-friendly-visual-design/05-UI-SPEC.md
@.planning/phases/05-kid-friendly-visual-design/05-PATTERNS.md
@.planning/phases/05-kid-friendly-visual-design/05-RESEARCH.md
@src/style.css
@src/main.ts
@src/ui/components/ProgressIndicator.ts
@src/core/lessonEngine.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing tests for main.ts bug fixes, ThinkingIndicator, RewardToast, and completion-state ProgressIndicator</name>
  <files>tests/main.test.ts, tests/ui/components/ThinkingIndicator.test.ts, tests/ui/components/ProgressIndicator.test.ts</files>
  <read_first>
    - tests/ui/components/ProgressIndicator.test.ts (existing style: describe/it, createElement-based assertions, no mocking needed for pure render functions)
    - tests/e2e/lessonWalkingSkeleton.test.ts (mountApp mounting pattern: stub `fetch` to serve `public/Lesson-1A.json` from disk via `readFileSync`, `localStorage.clear()`, fresh `#app` div per test, `vi.spyOn(rewardAdvisorModule, "callRewardAdvisor")` mocked to a no-praise fallback so tests stay offline)
    - src/main.ts lines 47-294 (render() function: top-bar block lines 50-76, exercise submit handler lines 129-232, lesson-complete branch lines 233-290) — read in full to understand exact current branching before writing assertions against the FIXED behavior
    - src/ui/components/ProgressIndicator.ts (existing renderProgressIndicator/renderReviewProgressIndicator shape to mirror for the new renderProgressIndicatorComplete)
  </read_first>
  <behavior>
    - tests/ui/components/ProgressIndicator.test.ts (extend, add describe block): `renderProgressIndicatorComplete(19)` returns an element whose textContent is exactly `"Задание 19 из 19"` (never "20 из 19"), and it accepts only a `total` argument (no current/overshoot risk by construction).
    - tests/ui/components/ThinkingIndicator.test.ts (new file): `renderThinkingIndicator()` returns an `HTMLElement` with `className` containing `"thinking-indicator"`, `textContent` exactly `"Секунду, думаю…"`, and `getAttribute("role") === "status"` / `getAttribute("aria-live") === "polite"`.
    - tests/main.test.ts (new file), using the `mountApp` + stubbed-fetch + jsdom pattern from `tests/e2e/lessonWalkingSkeleton.test.ts`:
      - Test A (D-12 Gap 1 — feedback banner clears on advance): mount app, advance through theory ("Понятно"), submit a correct main-pass text-input answer, assert `.feedback-banner` is present immediately after advance; then trigger a SECOND `render(store.getState())` for the new current exercise with no new submit (e.g. by dispatching an unrelated store update, or by directly re-invoking the subscribed render callback) and assert `.feedback-banner` is ABSENT on that second render — proving the transient `feedback` state was nulled after being consumed once, not left to leak onto every subsequent render of the same or a later exercise.
      - Test B (D-12 Gap 2 — progress indicator no overshoot): mount app, drive the engine through all exercises (submit correct answers) until `engine.getCurrentExercise()` returns null (lesson complete, reviewQueue empty — use a lesson fixture path or directly manipulate state via repeated correct submits, mirroring `tests/e2e/fullLessonTraversal.test.ts`'s traversal approach if faster), assert the top-bar's progress-indicator element textContent is `"Задание 19 из 19"` (or the completion-specific variant's text), and explicitly assert it is NOT `"Задание 20 из 19"` or any `N+1`-shaped string.
      - Test C (ruble balance chip, UI-02): mount app, assert a `.ruble-balance` element exists in the top bar with textContent matching `` `${state.currentRewards} ₽` `` for the initial state (e.g. `"0 ₽"` on a fresh profile), matching the exact live value read from state (not hardcoded).
      - Test D (thinking-indicator wiring, at least 1 of the 3 call sites — theory buttons is cheapest/fastest to test): mount app, tap "Понятно", and assert a `.thinking-indicator` element is appended to the DOM synchronously after the click (before the awaited `handleTheoryStep()` resolves) — use `vi.spyOn`/a delayed mock on the relevant agent call if needed to create an observable window, or assert the indicator is removed once `vi.waitFor` confirms the theory screen has advanced.
      - Test E (reward toast, D-10): mount app, drive to a correct text-input answer that is known to trigger a reward event (per `tests/e2e/*` fixtures' established reward-triggering inputs), and assert a `.reward-toast` element appears in the DOM with textContent matching `` `+${N} ₽` `` where N equals the actual `state.currentRewards` delta before/after the submit — do NOT hardcode N; read it from the state diff within the test itself.
    - All new tests MUST fail against current code (RED) before Task 2 implements the fixes/components (GREEN) — confirm every new/extended test file fails for the RIGHT reason (missing export/element/behavior, not a syntax error) before proceeding.
  </behavior>
  <action>
    Write the test files per the &lt;behavior&gt; block above, mirroring `tests/ui/components/ProgressIndicator.test.ts`'s plain describe/it style for pure-render-function assertions, and `tests/e2e/lessonWalkingSkeleton.test.ts`'s `mountApp`+stubbed-fetch+mocked-agent pattern for the `tests/main.test.ts` DOM-integration assertions. Import `renderProgressIndicatorComplete` and `renderThinkingIndicator` from their (not-yet-created) module paths — these imports will fail to resolve until Task 2, which is expected RED state. For Test B/E's "drive through exercises" step, reuse the exact correct-answer input values already established in `tests/e2e/fullLessonTraversal.test.ts` (read that file first if the exact accepted answers per exercise are needed) rather than guessing new ones. Run `npm test -- tests/main.test.ts tests/ui/components/ThinkingIndicator.test.ts tests/ui/components/ProgressIndicator.test.ts` and confirm all new/changed assertions fail (RED) for missing-implementation reasons, not test-authoring bugs.
  </action>
  <verify>
    <automated>npm test -- tests/main.test.ts tests/ui/components/ThinkingIndicator.test.ts tests/ui/components/ProgressIndicator.test.ts 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - tests/main.test.ts exists and contains Tests A-E described above, all currently failing (RED) due to missing implementation, not test syntax errors
    - tests/ui/components/ThinkingIndicator.test.ts exists, failing on missing module
    - tests/ui/components/ProgressIndicator.test.ts extended with a `renderProgressIndicatorComplete` describe block, failing on missing export
  </acceptance_criteria>
  <done>All 5 new/extended test scenarios exist, run, and fail for the correct reason (RED state confirmed via `npm test` output showing assertion/import failures, not runtime crashes unrelated to the feature).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement design tokens, ThinkingIndicator, RewardToast, ruble chip, and both D-12 bug fixes (GREEN)</name>
  <files>src/style.css, src/main.ts, src/ui/components/ThinkingIndicator.ts, src/ui/components/RewardToast.ts, src/ui/components/ProgressIndicator.ts</files>
  <read_first>
    - .planning/phases/05-kid-friendly-visual-design/05-UI-SPEC.md (Color, Typography, Spacing Scale, Shape Language & Components, Motion Contract, Copywriting Contract sections — every exact hex/px/ms/copy value used below comes from here verbatim, do not re-derive)
    - .planning/phases/05-kid-friendly-visual-design/05-PATTERNS.md (src/main.ts section: exact 3 call-site locations for the thinking-indicator insertion, the corrected reward-toast trigger logic using a before/after `state.currentRewards` diff — NOT a `result.rewardAmount` field, which does not exist on `HandleAnswerResult`)
    - src/main.ts (full file, already read this session — re-derive edits against the line ranges cited in 05-PATTERNS.md: top-bar block 50-76, theory handler 85-108, exercise submit handler 129-232, "Показать итоги" handler 266-289)
    - src/style.css (full file, already read this session)
  </read_first>
  <action>
    In `src/style.css`: update `:root` token VALUES per 05-UI-SPEC.md Color table — bright, saturated, primary-color-forward palette per D-01, extending the existing 60/30/10 token NAMES with new VALUES per D-02 (`--color-bg: #FFF8E7`, `--color-bg-secondary: #DCEEFF`, `--color-accent: #2E7DF7`, `--color-error: #E8544A`, `--color-success: #3DBB5E`) and ADD new custom properties: `--color-accent-dark: #1E5FC7`, `--color-error-dark` and `--color-success-dark` (~15-20% darker than their base per UI-SPEC's border-color technique — error/success stay the only 2 functional signal colors distinct from the brand accent per D-03, error reserved for banner border/icon only, never a harsh full-bleed fill), `--color-highlight: #FFC93C` and `--color-highlight-dark` (darker gold shade), plus `--space-2xl: 48px` and `--space-3xl: 64px`. Update typography roles (`.heading` to 22px/700, `.display` to 32px/700, `.label` to 14px/700; `body`/base stays 16px/400/1.5) per UI-SPEC Typography table and D-08 (Phase 1's sizes are a FLOOR, not to be gone below) — retire weight 600 everywhere it appears (search for `font-weight: 600` and replace with `700`), keeping the existing system-font stack unchanged per D-07 (no new webfont/network dependency). Update the base `button` rule: `border-radius: 14px` (D-04's 12-16px blocky-not-circular range), keep `min-height: 44px`/`min-width: 44px` as the floor (D-06's touch-target floor — do NOT bump this globally, see Pitfall 3), `font-weight: 700`. Add a NEW scoped rule for primary CTAs only (target `.submit-row button.accent`, `.show-results-button`, and a `.continue-button` class) applying D-06's primary-CTA exception: `min-height: 52px`, `padding: 0 var(--space-lg)`, `border: 3px solid var(--color-accent-dark)` (D-04's thick-darker-shade-border technique), `box-shadow: 0 4px 0 var(--color-accent-dark)`, and a `:active` variant shifting `transform: translateY(4px)` + `box-shadow: 0 0 0 var(--color-accent-dark)` (D-06's visible tactile "pressed" state, per 05-PATTERNS.md Pattern 2 / RESEARCH.md's zero-blur offset-shadow technique) — verify this selector does NOT match `.option`, `.match-left`, `.match-right`, `.bank-chip`, `.sequence-chip`, or theory's plain `.theory-buttons button` (those keep the 44px floor). Update `.feedback-banner.correct`/`.incorrect` to reference the new color values; add `@keyframes bounce-in` (scale 0.9→1.05→1, 350ms ease-out) applied to `.feedback-banner.correct`, and `@keyframes gentle-shake` (translateX -4px/4px/-2px/0, 300ms ease-in-out, single pass, no `infinite`) applied to `.feedback-banner.incorrect` — both are lightweight CSS-only motion per D-09, the shake calm/brief/non-punitive rather than harsh. Add new component classes, all plain CSS shapes/emoji per D-05 (no icon library, no third-party asset package): `.thinking-indicator` (small pulsing circle, `@keyframes pulse { opacity 0.4→1→0.4 }`, 1000ms ease-in-out infinite, plus a `@media (prefers-reduced-motion: reduce)` override setting `animation: none; opacity: 0.7`) — the SAME shared class/component reused across all 3 agent-wait call sites per D-09, never 5 bespoke indicators, `.reward-toast` (background `var(--color-highlight)`, border `3px solid var(--color-highlight-dark)`, border-radius 14px, `@keyframes reward-toast-in` translateY 16px→0 + opacity 0→1, 250ms ease-out on enter) — the visual celebration of an already-decided reward event per D-10, never new reward logic, and `.ruble-balance` (a small chip: `--color-highlight` background or a lighter tint, border-radius 14px, padding `var(--space-xs) var(--space-sm)`). Activate `--space-2xl`/`--space-3xl` on `.session-end-screen` (top padding `var(--space-3xl)`, section padding `var(--space-2xl)`) per UI-SPEC's hero-spacing note — this class already exists in `SessionEndScreen.ts`, only the CSS rule is new. This task covers the theory/exercise/feedback/top-bar/session-end screens in the SAME pass as the shared chrome, consistent with D-11's "all screens in one pass" (Wave 2 completes the remaining per-renderer chip/option surfaces).

    Create `src/ui/components/ThinkingIndicator.ts` mirroring `FeedbackBanner.ts`'s shape (zero-argument factory): `export function renderThinkingIndicator(): HTMLElement` returns a `div` with `className = "thinking-indicator"`, `role="status"`, `aria-live="polite"`, `textContent = "Секунду, думаю…"` (per UI-SPEC Copywriting Contract) — `createElement`/`textContent`/`setAttribute` only, zero innerHTML.

    Create `src/ui/components/RewardToast.ts`: `export function renderRewardToast(amount: number): HTMLElement` returns a `div` with `className = "reward-toast"`, `textContent = \`+${amount} ₽\`` — pure render function, no internal timer (caller owns mount/auto-dismiss via `setTimeout`).

    Extend `src/ui/components/ProgressIndicator.ts`: add `export function renderProgressIndicatorComplete(total: number): HTMLElement` as a third sibling function (same shape as the existing two), `className = "label progress-indicator progress-indicator-complete"`, `textContent = \`Задание ${total} из ${total}\`` (never `total + 1`).

    In `src/main.ts`:
    1. Import `renderThinkingIndicator` from `./ui/components/ThinkingIndicator`, `renderRewardToast` from `./ui/components/RewardToast`, and `renderProgressIndicatorComplete` from `./ui/components/ProgressIndicator` (add to the existing named-import line).
    2. Top-bar ruble chip: immediately after appending `title` to `topBar` (after line 55), create a `span` with `className = "ruble-balance"` and `textContent = \`${state.currentRewards} ₽\`` (per 05-PATTERNS.md's exact pattern), append to `topBar`.
    3. Top-bar D-12 Gap 2 fix: replace the `else` branch at lines 67-74 (the non-review-pass progress-indicator append) with a completion check reusing `engine.getCurrentExercise()` as the single completion signal — if it returns null/undefined, append `renderProgressIndicatorComplete(engine.totalExercises)` instead of the overshoot-prone `renderProgressIndicator(currentExerciseIndex + 1, totalExercises)`. Per 05-PATTERNS.md's note, this call to `getCurrentExercise()` duplicates the one made later inside the `main` content block (line 118) — acceptable (pure getter, no side effect); do not attempt to hoist/dedupe across the two blocks in this task, keep the diff minimal and scoped to the top-bar block only.
    4. Thinking-indicator at 3 call sites, inserting inside the EXISTING try/finally at each (do not restructure the disable/unsubscribe/await/finally shape): (a) theory buttons handler (~lines 94-104) — append `renderThinkingIndicator()` to `theoryNode` right after disabling buttons, remove it in the `finally` block; (b) exercise submit handler (~lines 135-156) — append to `exerciseNode` right after disabling `submitButton`, remove in `finally`; (c) "Показать итоги" handler (~lines 271-283) — append to `main` right after disabling `showResultsButton`, remove in `finally`.
    5. Reward toast trigger (D-10, corrected per 05-PATTERNS.md — `HandleAnswerResult` has NO `rewardAmount` field): in the exercise submit handler, capture `const rewardsBefore = store.getState().currentRewards;` immediately before the `try` block (before the `unsubscribeRender()` call), and after `result = await engine.handleAnswer(...)` resolves (still inside the `try`, after the await, before `finally`), compute `const rewardsDelta = store.getState().currentRewards - rewardsBefore;` and if `rewardsDelta > 0`, append `renderRewardToast(rewardsDelta)` to `document.body` (not `main`, since it must survive the DOM rebuild that `render()` may trigger) with `setTimeout(() => toastEl.remove(), 1950)` (250ms enter + 1500ms hold + 200ms exit per UI-SPEC Motion Contract). This diff read is safe here per 05-PATTERNS.md's established single-dispatch-window invariant (nothing else calls `store.dispatch` during this await).
    6. D-12 Gap 1 fix (feedback banner not cleared on advance): add an explicit `feedback = null` reset so the transient object does not outlive the one render it was captured for. Minimal-diff approach: at the top of the two "correct answer, advance" branches (lines 171-174 main-pass correct, lines 175-182 review-pass correct) — AFTER `render(store.getState())` is called (so the render that legitimately shows the banner for the just-completed advance still sees it), set `feedback = null` so any LATER render (not caused by a new submit) never re-shows a stale banner. Verify this does not break the existing `feedbackAppliesHere` checks at lines 226-229 and 241-244 for the SAME render call that intentionally shows the banner (order the `render()` call before the `feedback = null` assignment, not after).

    Run `npm test -- tests/main.test.ts tests/ui/components/ThinkingIndicator.test.ts tests/ui/components/ProgressIndicator.test.ts` and confirm all tests from Task 1 now pass (GREEN). Then run the full suite once to confirm no regression in existing e2e/core tests.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tests/main.test.ts tests/ui/components/ThinkingIndicator.test.ts tests/ui/components/ProgressIndicator.test.ts` passes (all Task 1 RED tests now GREEN)
    - Full `npm test` suite passes with zero regressions in existing `tests/e2e/*`, `tests/core/*`, and other `tests/ui/*` files
    - `grep -c "innerHTML" src/ui/components/ThinkingIndicator.ts src/ui/components/RewardToast.ts` returns 0 for both new files
    - `grep -n "font-weight: 600" src/style.css` returns no matches (all bumped to 700)
  </acceptance_criteria>
  <done>All 5 must-have truths verified: ruble chip live in top bar, thinking-indicator shared across all 3 call sites, reward toast fires on real reward delta, feedback banner never leaks to an unrelated render, progress indicator never overshoots at lesson-complete. Full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new this plan | This plan adds zero new user-input surfaces, zero new external calls, zero new persisted fields. All new DOM content (thinking-indicator text, reward-toast "+N ₽", ruble-balance chip) is either static copy or numeric values already validated/typed by the core (`state.currentRewards: number`, computed reward deltas from already-granted `RewardEvent` records). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | ThinkingIndicator.ts, RewardToast.ts, main.ts ruble-chip insertion | low | mitigate | Continue `createElement`/`textContent`/`setAttribute` exclusively for every new element — zero `innerHTML` anywhere in this plan's new code, verified via `grep -c innerHTML` gate in Task 2's acceptance criteria (same project-wide discipline as Phases 1-4, no new attack surface introduced). |
| T-05-02 | Information Disclosure | reward-toast delta computation (before/after `state.currentRewards` diff) | low | accept | The diff only reads already-persisted, already-validated numeric state the child's own session produced (no cross-user or cross-session data); no new information is exposed beyond what SessionEndScreen already displays via the same field. |

No new attack surface: this is a presentational-only re-skin plus two bug fixes to existing render logic — same DOM-construction discipline as all prior phases, no new data flow, no new agent calls, no new user input handling.
</threat_model>

<verification>
- `npm test -- tests/main.test.ts tests/ui/components/ThinkingIndicator.test.ts tests/ui/components/ProgressIndicator.test.ts` green (scoped, fast)
- `npm test` (full suite) green — confirms zero regression across `tests/core/*`, `tests/e2e/*`, `tests/ui/*`
- Manual: `npm run dev`, walk theory → first exercise → correct answer, visually confirm bright cream/blue palette, chunky button shadow/press, ruble chip in top bar, thinking cue on tap, reward toast on a reward-triggering correct answer
</verification>

<success_criteria>
- All 5 must-have truths hold (see frontmatter `must_haves.truths`)
- `src/style.css` token values match 05-UI-SPEC.md's Color/Typography/Spacing/Shape Language tables exactly
- `src/main.ts` has exactly 3 thinking-indicator call sites, 1 ruble-chip insertion, 1 reward-toast trigger, both D-12 fixes
- Zero new dependencies added to `package.json`
</success_criteria>

<output>
Create `.planning/phases/05-kid-friendly-visual-design/05-01-SUMMARY.md` when done
</output>
