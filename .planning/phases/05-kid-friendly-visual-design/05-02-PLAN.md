---
phase: 05-kid-friendly-visual-design
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/style.css
  - src/ui/exercise-renderers/textInput.ts
  - src/ui/exercise-renderers/singleChoice.ts
  - src/ui/exercise-renderers/matching.ts
  - src/ui/exercise-renderers/orderBuilder.ts
  - src/ui/screens/TheoryScreen.ts
  - tests/ui/exercise-renderers/singleChoice.test.ts
  - tests/ui/exercise-renderers/matching.test.ts
  - tests/ui/exercise-renderers/orderBuilder.test.ts
  - tests/ui/screens/TheoryScreen.test.ts
autonomous: false
requirements: [UI-01, UI-02]

must_haves:
  truths:
    - "Every exercise-type renderer (text-input, single-choice, matching, order-builder) presents its interactive elements (options, chips, match buttons) using the new chunky/blocky component classes, distinguishable in a screenshot from Phase 1's plain rectangles"
    - "Chips and secondary/option buttons keep the 44px touch-target floor and do NOT inherit the 52px primary-CTA-only height exception"
    - "Theory screen's Понятно/Не понятно buttons render with the new typography/shape styling and are visually distinct from the (separately-styled) primary submit CTA"
    - "No screen or renderer introduces a UI framework, icon library, or innerHTML usage"
  artifacts:
    - "Updated classNames/structure (if any) in all 4 exercise-renderer files and TheoryScreen.ts consistent with 05-UI-SPEC.md Shape Language"
  key_links:
    - "All 4 exercise renderers and TheoryScreen consume the SAME CSS custom properties and component classes defined in 05-01's style.css — no renderer redefines its own color/shape values"
</must_haves>
---

<objective>
Apply the Phase 5 visual identity (chunky borders, darker-shade shadows, 14px radius, bright saturated option/chip colors) to the 4 exercise-type renderers and the theory screen's toggle buttons, using the design tokens and shared component classes established in Plan 01. Adds the small number of new classNames/CSS rules needed for chip/option-specific styling (`.option`, `.match-left`, `.match-right`, `.bank-chip`, `.sequence-chip`) that Plan 01 intentionally left untouched (Plan 01 only updated the shared base `button`/`.accent`/task-card/feedback-banner rules, plus the CTA-only exception scoped to `.submit-row button.accent`/`.show-results-button`/`.continue-button`).

Purpose: completes D-11's "all screens get the skin in one pass" requirement — Plan 01 covered shared chrome (top bar, tokens, thinking-indicator, reward toast, feedback banner motion); this plan covers the per-exercise-type interactive surfaces that make up the bulk of the child's screen time.
Output: Updated CSS rules for chip/option/match-button classes in `src/style.css`, confirmed-correct touch-target floors across all 4 renderers, re-skinned theory toggle buttons, extended DOM-assertion tests confirming the new classNames are present without altering existing interaction-mechanic tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-kid-friendly-visual-design/05-UI-SPEC.md
@.planning/phases/05-kid-friendly-visual-design/05-PATTERNS.md
@src/ui/exercise-renderers/textInput.ts
@src/ui/exercise-renderers/singleChoice.ts
@src/ui/exercise-renderers/matching.ts
@src/ui/exercise-renderers/orderBuilder.ts
@src/ui/screens/TheoryScreen.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chip/option-specific CSS rules for all 4 exercise renderers (D-04 chunky styling, Pitfall 3 touch-target safety)</name>
  <files>src/style.css</files>
  <read_first>
    - .planning/phases/05-kid-friendly-visual-design/05-UI-SPEC.md Shape Language and Components section: border radius 14px, border width 3px darker-shade-of-own-color, no shadow except primary CTAs, Decorative shapes section for the plain CSS shapes allowance.
    - .planning/phases/05-kid-friendly-visual-design/05-PATTERNS.md Pitfall 3 section: scope the 52px CTA height to a dedicated class, do not bump base button min-height past 44px or chips/toggles will incorrectly inherit the CTA-only exception.
    - src/ui/exercise-renderers/singleChoice.ts (option className, lines 40 and 46), src/ui/exercise-renderers/matching.ts (match-left/match-right, lines 83 and 101), src/ui/exercise-renderers/orderBuilder.ts (bank-chip/sequence-chip, lines 70 and 80) - confirm exact existing classNames before adding CSS rules for them.
    - src/style.css as modified by Plan 01 - re-read after Plan 01 lands to confirm exact token names/values available: color-accent, color-accent-dark, color-bg-secondary, spacing tokens, and the exact selector Plan 01 used for the CTA-only 52px/shadow rule.
  </read_first>
  <action>
    Add CSS rules in src/style.css for the option/chip classNames Plan 01 did not touch (Plan 01 only updated the shared base button rule and the CTA-only exception scoped to the submit-row button.accent / show-results-button / continue-button selectors): .option (single-choice), .match-left and .match-right (matching), .bank-chip and .sequence-chip (order-builder). All five share the same visual treatment per D-04: border-radius 14px, a 3px solid border in a darker shade of the background color for the unselected state (add a new --color-bg-secondary-dark root token if Plan 01 did not already add one, using the same approximately 15-20 percent lightness-reduction formula as the other role pairs), background using --color-bg-secondary, generous padding using the sm/md spacing tokens, and an explicit 44px minimum height and width as the floor - never the 52px CTA exception (these selectors are structurally outside the submit-row block already, so they will not match Plan 01's CTA-only rule, but double check no other selector added in this task accidentally broadens that match). When an option or chip is selected, paired, or placed, the existing accent class each renderer's TS logic already toggles on selection continues to apply Plan 01's base button.accent/.selected treatment automatically - no TS change is needed for that, and this task only adds the UNSELECTED-state chunky border, radius, and background for the five classNames above. Per D-04, do not add a box-shadow to any of these five selectors, since shadow is reserved for primary CTAs only - confirm with a grep check that none of the five selectors appear in a rule block that also declares a box-shadow or a 52px minimum height.
  </action>
  <verify>
    <automated>npm test -- tests/ui/exercise-renderers 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/style.css contains new rules for .option, .match-left, .match-right, .bank-chip, .sequence-chip using --color-bg-secondary/--color-bg-secondary-dark and 14px radius/44px floor
    - none of those five selectors' rule blocks contain box-shadow or a 52px min-height declaration
    - existing exercise-renderer unit tests still pass unmodified (this task is CSS-only, no TS/DOM structure change)
  </acceptance_criteria>
  <done>All 5 chip/option classNames have chunky-styled CSS (14px radius, 3px darker-shade border, 44px floor, no shadow), none accidentally inherit the 52px primary-CTA exception, and no existing renderer test regresses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Theory toggle buttons get a distinct class from primary CTAs, plus SessionEndScreen hero-spacing class verification</name>
  <files>src/ui/screens/TheoryScreen.ts, tests/ui/screens/TheoryScreen.test.ts</files>
  <read_first>
    - src/ui/screens/TheoryScreen.ts full file (already read this session) - understoodButton/notUnderstoodButton creation at lines 50-58, currently plain buttons with no className beyond the base button element rule.
    - tests/ui/screens/TheoryScreen.test.ts full file (already read this session) - existing textContent-based button lookups (Ponyatno/Ne ponyatno), must keep passing unmodified in structure, only extended with a new className assertion.
    - .planning/phases/05-kid-friendly-visual-design/05-UI-SPEC.md Shape Language and Components table plus Color table's accent reserved-for list ("active/selected ponyatno/ne ponyatno toggle" is explicitly one of the accent-reserved uses) - confirms the UNDERSTOOD button, once tapped/active, uses the accent color, but this task's job is the default/idle visual treatment before selection, which per D-04/Pitfall 3 must NOT be the 52px CTA height (theory buttons are not primary CTAs - Проверить/Показать итоги/continue are the only 52px exceptions).
    - src/ui/screens/SessionEndScreen.ts full file (already read this session) - .session-end-screen/.child-section/.parent-section classNames already exist and already match the selectors Plan 01 activated 2xl/3xl spacing on; this task only needs to confirm (not modify) that no additional className is required for the hero-spacing CSS to apply, since Plan 01 already targeted .session-end-screen directly.
  </read_first>
  <behavior>
    - TheoryScreen.test.ts (extend): understoodButton and notUnderstoodButton both carry a shared className (e.g. "theory-toggle") distinct from the "accent"/CTA classNames used by exercise-submit buttons, verifiable via `el.querySelectorAll(".theory-toggle")` returning exactly 2 elements, and neither button's className contains the CTA-only marker class used for Проверить/Показать итоги/continue (whatever exact class Plan 01 scoped the 52px rule to - confirm the exact name by reading the Plan 01 SUMMARY or the current style.css before writing this assertion).
    - Existing 3 tests in TheoryScreen.test.ts (textContent/click-handler assertions) continue to pass unmodified.
  </behavior>
  <action>
    In src/ui/screens/TheoryScreen.ts, add `className = "theory-toggle"` to both understoodButton and notUnderstoodButton (in addition to their existing bare-button styling, so they keep the base button rule's font/radius/floor from Plan 01 but are structurally distinguishable from CTA buttons via a dedicated selector). Add a CSS rule in src/style.css (read Plan 01's final state first) for `.theory-toggle` if any theory-specific visual nuance is desired beyond the base button rule (e.g. explicit 44px floor confirmation) - keep this minimal, since D-04/UI-SPEC does not require theory toggles to look different from the base button treatment beyond NOT being 52px-tall. When a toggle is tapped and becomes the active/selected choice, the existing `.accent`/`.selected` class-toggle behavior (if any is added at the call site in main.ts - confirm this is out of scope for Phase 5 per 05-CONTEXT.md's structural-changes-not-required framing; if TheoryScreen.ts does not currently mark either button as selected/accent post-click, do not invent new selected-state logic here, since that would be a mechanics change beyond a re-skin - simply ensure the CSS treatment for `.theory-toggle` is correct for its current always-both-available structure). Confirm SessionEndScreen.ts requires zero code changes (Plan 01 already targets `.session-end-screen` directly in style.css) - do not edit SessionEndScreen.ts in this task unless the hero-spacing CSS genuinely cannot apply without a new className, which is not expected.
  </action>
  <verify>
    <automated>npm test -- tests/ui/screens/TheoryScreen.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/ui/screens/TheoryScreen.test.ts passes with the new .theory-toggle assertion added
    - both theory buttons carry className "theory-toggle" and do not carry the CTA-only 52px marker class
    - SessionEndScreen.ts is unmodified (confirmed via git diff showing no changes to that file this task)
  </acceptance_criteria>
  <done>Theory toggle buttons are visually distinguishable from primary CTAs via a dedicated className, all existing TheoryScreen tests plus the new className assertion pass, SessionEndScreen's hero spacing is confirmed working via Plan 01's existing .session-end-screen selector with no further edits needed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual visual walkthrough of the full re-skinned lesson flow</name>
  <files>(none — human verification of the running dev server)</files>
  <action>Pause for human verification. Run `npm run dev` and follow the steps in how-to-verify below; do not proceed until the user approves.</action>
  <verify>Human confirms each how-to-verify step behaves as described.</verify>
  <done>User types "approved" after confirming the bright/blocky visual identity, chunky CTAs, thinking indicator, reward toast, feedback animations, and both D-12 bug fixes all look and behave correctly in a real browser, with no resemblance to Roblox branding.</done>
  <what-built>
    All 4 exercise-type renderers and the theory screen now use the bright/blocky/chunky visual identity: warm-cream page background, saturated blue/yellow/green/red accents, 14px-radius chunky borders on every option/chip, the primary submit/continue/show-results CTAs at 52px height with a solid offset-shadow "pressed" effect, and theory toggles visually distinct from those CTAs. Combined with Plan 01's top bar (ruble balance chip), shared thinking-indicator, reward toast, bounce/shake feedback animations, and both D-12 bug fixes, this completes UI-01 and UI-02 for every screen in the lesson flow.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the app in a browser.
    2. Confirm the top bar shows the lesson title, a ruble-balance chip (e.g. "0 ₽"), and a progress indicator, all with the new bright/blocky styling.
    3. On the theory screen, confirm the rule/example text uses the new bold display typography, and the Понятно/Не понятно buttons look chunky (14px radius, visible border) but visually smaller/less prominent than a primary CTA.
    4. Tap "Понятно" - confirm a brief "Секунду, думаю…" thinking indicator appears, then the first text-input exercise renders with a large, chunky, shadowed "Проверить" button.
    5. Submit a correct answer - confirm a bounce/scale-in animation on the feedback banner, and if the answer triggers a reward, confirm a "+N ₽" toast slides up and auto-dismisses after about 2 seconds.
    6. Submit an incorrect answer - confirm a brief, gentle shake (not harsh) on the feedback banner, with a soft red border (never a full-bleed red background).
    7. Advance through single-choice, matching, and order-builder exercises - confirm all option/chip buttons share the same chunky-border, 44px-floor styling, and selected/paired/placed items turn accent-blue.
    8. Reach the end of the lesson - confirm the progress indicator reads "Задание 19 из 19" (never "20 из 19"), and the session-end screen shows the hero-spaced parent report with rubles earned.
    9. Confirm nothing on screen resembles Roblox's actual logo, wordmark, or brand red/white/black color scheme - this should read as a generic "bright blocky kids' game" aesthetic, not a copy of any specific brand.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new this plan | Pure CSS/className re-skin of existing exercise renderers and the theory screen. No new user input, no new external calls, no new persisted fields, no new DOM-insertion sites beyond a className addition on two existing buttons. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Tampering | TheoryScreen.ts className addition | low | accept | No new dynamic/user-controllable text is introduced; the added className is a static string literal, and all existing textContent-only rendering is unchanged. |

No new attack surface: this is a presentational-only re-skin (CSS + one static className addition) with the same createElement/textContent discipline as all prior phases - no new data flow, no new agent calls, no new user input handling.
</threat_model>

<verification>
- `npm test -- tests/ui/exercise-renderers tests/ui/screens/TheoryScreen.test.ts` green
- `npm test` (full suite) green after both tasks land
- Manual checkpoint (Task 3) approved by a human reviewer walking the full lesson flow in a real browser
</verification>

<success_criteria>
- All 4 exercise renderers' interactive elements (options, chips, match buttons) use the new chunky/blocky CSS classes and colors
- Chips/options retain the 44px touch-target floor; only the designated CTA classes (submit-row button.accent, show-results-button, continue-button) get the 52px height/shadow exception
- Theory toggle buttons are visually distinct from primary CTAs via a dedicated className
- Manual checkpoint approved, confirming no Roblox-branding resemblance and a coherent bright/blocky feel across every screen
</success_criteria>

<output>
Create `.planning/phases/05-kid-friendly-visual-design/05-02-SUMMARY.md` when done
</output>

